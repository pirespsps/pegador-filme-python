Projeto para o aprendizado de python, usando como base um projeto feito no ano passado em java.

A API utilizada: OMDB.


Bibliotecas necessárias:

requests:
-pip install requests

sqlite3:
-pip install sqlite3

pandas:
-pip install pandas
